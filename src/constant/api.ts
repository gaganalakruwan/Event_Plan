const baseUrl = 'https://jsonplaceholder.typicode.com/';

export const getPhotos = `${baseUrl}photos`;
export const getUsers = `${baseUrl}users`;
export const getAllPosts = `${baseUrl}posts`;
export const getAllComments = `${baseUrl}comments`;
